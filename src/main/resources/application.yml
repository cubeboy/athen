spring:
  application:
    name: athen
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
